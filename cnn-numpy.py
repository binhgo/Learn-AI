from __future__ import annotations
from typing import List, Dict, Callable, Optional
import time

import numpy as np


def forward_pass(self, a_prev: np.array) -> np.array:
    a_prev = np.array()
    a_prev.shape


def train(self, x_train: np.array, y_train: np.array, epochs: int, batch_size: int = 64) -> None:
# for e in range(epochs):
#     for x_batch, y_batch in generate_batches()
